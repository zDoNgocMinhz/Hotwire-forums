<div id="<%= dom_id(discussion) %>" class="mb-2">
    <h2><%= discussion.name %></h2>
    Pinned: <%= discussion.pinned %> | Closed: <%= discussion.closed %>
    <div>
      <%= link_to "Edit", edit_discussion_path(discussion) %> |
      <%= link_to "Destroy", discussion, data: { turbo_method: :delete, turbo_confirm: "Are you sure? This cannot be undone" } %>
    </div>
  </div>